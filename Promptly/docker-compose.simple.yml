version: '3.8'

services:
  promptly-web:
    build: .
    container_name: promptly-dashboard
    ports:
      - "5000:5000"
    volumes:
      - promptly-data:/data
    environment:
      - FLASK_ENV=production
      - PROMPTLY_DATA_DIR=/data
    restart: unless-stopped

volumes:
  promptly-data:
