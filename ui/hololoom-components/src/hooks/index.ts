/**
 * HoloLoom Hooks
 *
 * Custom hooks for animation, WebSocket, and state management.
 */

export {
  useAnimationFrame,
  animationLoop,
  RingBuffer,
  type AnimationFrameCallback,
  type UseAnimationFrameOptions,
  type AnimationFrameState,
} from './useAnimationFrame';
