{
  "task": "Explain TypeScript code clearly for beginners",
  "examples": [
    {
      "input": "type User = { name: string; age: number }",
      "output": "This defines a User type with two properties: name (text) and age (number)"
    },
    {
      "input": "const users: User[] = []",
      "output": "Creates an empty array that will store User objects"
    },
    {
      "input": "interface Product { id: number; price: number }",
      "output": "Defines a Product structure with id and price, both numbers"
    },
    {
      "input": "async function fetchData() { return await api.get() }",
      "output": "An asynchronous function that waits for and returns data from api.get()"
    }
  ],
  "inputs": ["code_snippet"],
  "outputs": ["explanation"]
}
