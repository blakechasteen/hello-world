/**
 * Webview entry point exports
 *
 * Note: These React components are currently placeholders.
 * The primary UI is implemented inline in the providers for simplicity.
 * These can be expanded for more complex UI needs.
 */

// Re-export webview entry points when built separately
// Currently, the webviews use inline HTML for simplicity
