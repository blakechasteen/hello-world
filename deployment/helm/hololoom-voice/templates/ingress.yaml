{{- if and .Values.voiceAgent.enabled .Values.voiceAgent.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "hololoom-voice.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "hololoom-voice.labels" . | nindent 4 }}
  {{- with .Values.voiceAgent.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.voiceAgent.ingress.className }}
  ingressClassName: {{ .Values.voiceAgent.ingress.className }}
  {{- end }}
  {{- if .Values.voiceAgent.ingress.tls }}
  tls:
    {{- range .Values.voiceAgent.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.voiceAgent.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "hololoom-voice.fullname" $ }}
                port:
                  number: {{ $.Values.voiceAgent.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
