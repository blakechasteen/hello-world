# Elle Game Engine Dependencies

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
httpx>=0.25.0  # For API calls and TestClient

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional LLM providers (install as needed)
# Anthropic Claude
anthropic>=0.21.0  # pip install anthropic

# OpenAI
openai>=1.12.0  # pip install openai

# Note: Local LLM (Ollama) requires no additional packages
# Just install Ollama from https://ollama.ai
